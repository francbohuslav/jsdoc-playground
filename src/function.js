/**
 * @param {string} text
 * @param {number} count
 * @returns {string}
 */
function repeatStr(text, count) {
  return text.repeat(count);
}

/**
 * @param {string} text
 * @param {number} count
 * @returns {string}
 */
const repeat2 = (text, count) => text.repeat(count);
